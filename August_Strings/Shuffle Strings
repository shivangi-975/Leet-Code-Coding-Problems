class Solution {
public:
    string restoreString(string s, vector<int>& indices) {
        string m=s;
        int n=indices.size();
        for(int i=0;i<n;i++)
        {
          m[indices[i]]=s[i];
         
          }
        return m;
    }
};
