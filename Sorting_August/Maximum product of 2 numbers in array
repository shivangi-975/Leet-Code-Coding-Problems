class Solution {
public:
    int maxProduct(vector<int>& nums) {
        int prod;
        int n=nums.size();
        sort(nums.begin(),nums.end());
        prod=(nums[n-1]-1)*(nums[n-2]-1);
        return prod;     
    }
};
